# Issue Verification — Batch 3

**Verified by:** Code read (no live testing)
**Date:** 2026-03-28
**App root:** /home/ubuntu/Claude-store/nexxus2.2_replit

| ID | Area | Still exists? | Evidence | Effort | Notes |
|----|------|--------------|----------|--------|-------|
| I-113 | Service metrics | YES | `service.tsx` lines 117-122: all six metric tiles have `change: 0, trend: 'up' as const`. Comment on line 107 acknowledges this as a known limitation. No delta calculation logic exists. | M | Needs time-series comparison query on backend (e.g., current vs previous period counts). |
| I-115 | SubMenu/tab mismatch | FIXED | SubMenuManager.tsx service case (lines 580-583) offers: Campaigns, Agents, Insights, Calendar. service.tsx tabs (lines 60-65): campaigns, agents, insights, calendar. Marketing submenu (lines 605-607): Dashboard, Studio, Insights + MARKETING_AGENTS list. marketing.tsx tabs (lines 40-45): dashboard, agents, studio, insights. Submenu omits "Agents" tab link but agents are accessible via the inline MARKETING_AGENTS launcher list. Management submenu (lines 655-659): insights, hunches, activities, user-chats, billing. management.tsx tabs (lines 38-44): insights, hunches, activities, user-chats, billing. All match. | - | Marketing submenu lists 3 nav links (no explicit "agents" tab link) but the MARKETING_AGENTS section provides direct agent access. Minor UX gap, not a mismatch. |
| I-119 | Widget voice/call mode | FIXED | widget-landing.tsx lines 462-473: Voice option is now labeled "Instant Call Back" and "Get a call back now". Lines 642-700: voice mode renders a phone number input field and calls `submitCallback()` which POSTs to `/api/widget/voice-callback`. This is a callback flow, NOT a browser VAPI call. However, the `startVoiceCall()` function (lines 278-298) still exists and performs a VAPI browser call — it is just no longer invoked from the menu. | S | The old VAPI browser call function `startVoiceCall()` is dead code. The voice-callback backend endpoint does NOT exist in server/routes/public.ts — will 404. See I-122. |
| I-121 | Video window.open popup blocker | FIXED | widget-landing.tsx line 333: `const videoWindow = window.open('about:blank', '_blank')` is called SYNCHRONOUSLY at the top of `startVideoChat()`, before any async work. Same pattern on line 114 for auto-launch. This prevents popup blocker issues. | - | Correctly implemented. |
| I-122 | Web Call widget old VAPI browser call | FIXED (UI) / PARTIAL (backend) | The widget menu (line 463) now launches the "Instant Call Back" flow (phone number input + `/api/widget/voice-callback` POST). The old VAPI browser call is no longer reachable from the UI. However, `/api/widget/voice-callback` endpoint does NOT exist in server code — `grep` across `/server` returns zero matches. The `submitCallback()` function (lines 309-326) will get a 404. | M | Backend endpoint `/api/widget/voice-callback` needs to be created. It should trigger an outbound call via VAPI to the provided phone number. |
| I-123 | Widget form creates conversation | FIXED | `server/routes/public.ts` lines 66-118: `/api/widget/contact` handler creates a conversation via `storage.createConversation()` (line 93) with channel "form", then creates a message via `storage.createMessage()` (line 106). Returns `{ success: true, conversationId }`. Will appear in TeamBox. | - | Confirmed working. |
| I-124 | Marketing popout duplicate agents | FIXED | SubMenuManager.tsx line 635: explicit comment `/* I-124: Removed duplicate agent list call — MARKETING_AGENTS section above is the consolidated agent list */`. Only the MARKETING_AGENTS section (lines 610-633) renders agents. No `renderAgentList('marketing')` call. | - | Confirmed fixed. |
| I-128 | Campaign Safety no dismiss button | FIXED | service.tsx line 91: `campaignSafetyDismissed` state initialized from localStorage. Lines 622-641: safety card is conditionally rendered with `{!campaignSafetyDismissed && (`. Lines 636-641: dismiss button exists with `setCampaignSafetyDismissed(true)` and `localStorage.setItem('campaign-safety-dismissed', 'true')`. Has `data-testid="button-dismiss-campaign-safety"`. | - | Confirmed fixed with localStorage persistence. |
| I-129 | Campaign action button tooltips | FIXED | service.tsx lines 423-507: all campaign action buttons wrapped in `<Tooltip>` + `<TooltipTrigger>` + `<TooltipContent>`. Labels: "Stop Execution" (435), "Execute Campaign" (452), "Schedule" (469), "Dry Run" (483), "Upload CSV" (506). | - | Confirmed fixed. |
| I-132 | Campaign multi-channel support | YES | service.tsx line 138: `const [newCampaignChannel, setNewCampaignChannel] = useState('sms')`. Line 572: campaign creation sends single `channel: newCampaignChannel`. Lines 521-524 comment confirms: "I-132: Multi-channel campaign support... Implementing multi-channel requires backend changes to the campaign model to store an array of channels." `outbound.ts` line 592: `const contactChannel = campaign.channel as "sms" \| "email" \| "phone"` — single value. | L | Requires schema change (channel -> channels array), UI multi-select, and execution loop per-channel. |
| I-134 | Landing page route redirect race | FIXED | App.tsx lines 41-49: `PublicRouter` handles `/w/:slug` and `/p/:slug` in a separate `<Switch>`. Lines 101-103: `isPublicRoute()` checks path prefix. Lines 113-127: public routes render WITHOUT `<AuthProvider>`, authenticated routes render inside `<AuthProvider>`. Comment on line 39 explicitly references "Fixes I-134". No catch-all race possible. | - | Confirmed fixed. Clean separation of public and auth routes. |
| I-135 | Widget CORS blocks cross-origin | FIXED | server/index.ts lines 60-63: `widgetCors = cors({ origin: '*', methods: ['GET', 'POST', 'OPTIONS'], allowedHeaders: ['Content-Type'] })` applied to both `/api/widget` and `/widget` paths. Lines 67-69: general CORS middleware skips widget paths to avoid overriding. Comment on line 59 references "I-135 fix". | - | Confirmed fixed. |
| I-143 | No business-hours gate on outbound | FIXED | outbound.ts lines 224-235: `isWithinBusinessHours()` function using org timezone (defaults America/New_York) and configurable start/end hours (defaults 08:00-21:00 TCPA window). Lines 266-274: `checkCommGate()` calls this for SMS and phone channels, blocking sends outside hours with descriptive reason. | - | Confirmed fixed. Configurable per-org via `settings.timezone`, `businessHoursStart`, `businessHoursEnd`. |
| I-144 | Blacklist not checked in CommGate | FIXED | outbound.ts lines 288-295: `checkCommGate()` includes SMS blacklist check — calls `storage.getBlacklistEntry(customerContact, org.id)` and blocks with reason "Recipient blacklisted". Note: blacklist check is SMS-only in CommGate. The `sendSms()` function (lines 99-104) also has its own blacklist check. Phone and email channels are not blacklist-checked. | S | Blacklist check only applies to SMS channel in CommGate. Phone/email blacklist is not implemented. Consider extending to all channels. |
| I-168 | Widget interaction mode states | NEEDS LIVE TEST | Cannot verify via code read. widget-landing.tsx defines `WidgetMode = 'closed' \| 'chat' \| 'video' \| 'voice' \| 'form' \| 'menu'` (6 modes). State transitions exist for all modes. Dead code: `startVoiceCall()` VAPI browser call function is unreachable. Missing backend: `/api/widget/voice-callback` endpoint. | - | Requires Playwright or manual browser testing across all 6 modes x transitions x error states. Voice callback will fail (no endpoint). |

## Summary

| Status | Count |
|--------|-------|
| Still exists | 2 (I-113, I-132) |
| Fixed | 10 (I-115, I-119, I-121, I-123, I-124, I-128, I-129, I-134, I-135, I-143) |
| Partially fixed | 2 (I-122, I-144) |
| Needs live test | 1 (I-168) |

## Critical Finding

**I-122 / I-119 backend gap:** The widget voice mode now correctly shows an "Instant Call Back" UI that collects a phone number and POSTs to `/api/widget/voice-callback`. However, this endpoint does not exist on the server. All callback requests will return 404. This must be implemented before the callback feature is functional.

**I-144 partial:** Blacklist is only checked for SMS in CommGate. Phone and email outbound channels bypass blacklist entirely.

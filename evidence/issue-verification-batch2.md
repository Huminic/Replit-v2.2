# Issue Verification Batch 2

**Date:** 2026-03-28
**Verifier:** Code-read verification (no live testing)
**App root:** /home/ubuntu/Claude-store/nexxus2.2_replit

| ID | Area | Still exists? | Evidence | Effort | Notes |
|----|------|---------------|----------|--------|-------|
| I-117 | TopBar Profile dropdown | **NO — Fixed** | TopBar.tsx line 370 now reads `Reset Tour`, not "Take a Tour". The `data-testid` is still `menu-item-take-tour` (cosmetic, non-blocking). | — | Closed |
| I-118 | TopBar Profile dropdown | **NO — Fixed** | TopBar.tsx lines 360-376: dropdown contains only My Profile, Preferences, Reset Tour, Log out. No Billing link exists in the profile dropdown. | — | Closed |
| I-120 | AI Config tile RBAC | **NO — Consistent** | settingsTiles array (settings.tsx line 304): AI Config `minRole: ['super_admin', 'partner_admin']`. SubMenuManager.tsx line 672: AI Config `roles: ['super_admin', 'partner_admin']`. Both tile and sub-menu use the same role gate. No inconsistency found. | — | Closed (tile and sub-menu are aligned) |
| I-137 | ProductTour skip/close nav | **NO — Fixed** | ProductTour.tsx: `onSkip` and `onComplete` callbacks do NOT navigate. AppLayout.tsx lines 97-107: both `handleTourComplete` and `handleTourSkip` only call `localStorage.setItem(...)` and `setShowTour(false)`. No `setLocation` or `navigate` call. No navigation occurs on skip/close. | — | Closed |
| I-140 | Password reset backend | **NO — No obvious bug** | server/routes/auth.ts lines 413-455: reset-password endpoint hashes incoming token with SHA-256, calls `findUserByResetToken(tokenHash)` which queries DB by `resetToken` column, checks expiry, hashes new password with bcrypt, clears token, deletes sessions. storage.ts lines 1423-1432 confirm both `findUserByResetToken` and `clearResetToken` are implemented. No code-level bug visible. | 1h | NEEDS LIVE TEST to confirm end-to-end (email delivery depends on RESEND_API_KEY + CommGate). |
| I-148 | Role Switcher dev tool | **YES — Still present** | TopBar.tsx file header (lines 12-13) explicitly documents "Role Switcher: DEV TOOL — allows switching RBAC role for testing. Shows all 8 roles. Will be removed or restricted in production." The import list (lines 29-41) does not include ArrowDownRight, suggesting the role switcher may have been removed from the rendered JSX while the comment remains. Full JSX (lines 137-382) contains no role switcher UI element — only the comment/docblock references it. | 0.5h | **Partially resolved**: The role switcher UI appears to have been removed from the rendered output, but the docblock comments at lines 12-13, 69, and 79 still reference it. Needs cleanup of stale comments. |
| I-149 | Tour overlay reappears | **YES — By design (per-page)** | AppLayout.tsx lines 83-92: tour dismissed state is stored per-page in localStorage with key `nexxus_tour_dismissed_{pageKey}`. On each navigation, it checks if the NEW page's tour was dismissed. If not, it shows the tour after 800ms delay. This means tour appears once per unique top-level route segment, not once globally. | 1h | If the intent is "show tour once ever", this is a bug. If "show tour once per section", this is working as designed. Clarify product intent. |
| I-151 | Settings tile count | **YES — Only 7 tiles** | settings.tsx lines 299-307: `settingsTiles` array defines exactly 7 tiles: User Management, Organization, Tools & Integrations, Knowledge Base, AI Configuration, Notifications, Appearance. Visible count depends on role filtering (line 969). For super_admin: 7 tiles. For partner_admin: 7 tiles. For org_admin: 5 tiles (AI Config and its partner-only gate excluded, but actually AI is partner+super only, so org_admin sees 5). The original issue says "only 4 tiles" — this may have been fixed by adding tiles, or the reporter was on a restricted role. | 0.5h | NEEDS LIVE TEST to confirm actual rendered count per role. Code shows 7 defined, 5-7 visible depending on role. |
| I-157 | API Keys & Webhooks RBAC | **YES — super_admin gated** | settings.tsx line 2881-2882: `{isSuperAdmin && <TabsTrigger value="api-keys" ...>}` and `{isSuperAdmin && <TabsTrigger value="webhooks" ...>}`. These tabs are gated to super_admin only via `isSuperAdmin` (line 957: `const isSuperAdmin = currentRole === 'super_admin'`). If the requirement is for these to be visible to other roles, this is a bug. If super_admin-only is intended, this is correct. | 0.5h | Confirm intended RBAC level for API Keys and Webhooks tabs. Currently super_admin only. |
| I-164 | Settings interaction states | **NEEDS LIVE TEST** | 42 interaction states across settings sub-sections. This is a test coverage gap, not a code bug. Cannot verify by code reading alone. | 4-6h | Full interaction state verification sprint needed. Requires systematic click-through of all settings sections with multiple roles. |
| I-165 | Forgot/reset password FE | **Pages exist, NEEDS LIVE TEST** | `client/src/pages/forgot-password.tsx` and `client/src/pages/reset-password.tsx` both exist. 11 states untested (form validation, error handling, success flow, expired token, etc.). | 2-3h | FE pages present. Backend endpoints confirmed working at code level. Needs end-to-end flow test. |
| I-166 | Org wizard states | **Page exists, NEEDS LIVE TEST** | `client/src/pages/org-wizard.tsx` exists. Settings.tsx line 1078 shows button linking to it (super_admin gated). 11 states untested. | 2-3h | Page exists and is reachable. Needs systematic state verification. |
| I-172 | AgentChatView openai-proxy 401 | **YES — No token refresh on 401** | AgentChatView.tsx line 404-416: calls `getAccessToken()` from tokenStore (a simple getter, returns in-memory token). On 401, the code throws `new Error('API error: 401')` with no retry or token refresh logic. If the access token expires mid-chat, the request fails with no recovery. The broader auth system has refresh via httpOnly cookie, but AgentChatView does not invoke it before retrying. | 2h | Bug confirmed: no automatic token refresh on 401 in AgentChatView. Needs either a fetch wrapper that auto-refreshes on 401 or explicit refresh-then-retry logic. |

## Summary

| Status | Count | IDs |
|--------|-------|-----|
| Fixed / Closed | 4 | I-117, I-118, I-120, I-137 |
| No code bug found (needs live test) | 1 | I-140 |
| Still exists (code confirmed) | 2 | I-149 (by-design?), I-172 |
| Needs clarification | 2 | I-148 (stale comments only), I-157 (intended RBAC?) |
| Needs live test only | 4 | I-151, I-164, I-165, I-166 |
